[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "realtime-decoding"
version = "0.1.0"
description = "Realtime decoding experiments for QLDPC codes"
readme = "README.md"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]